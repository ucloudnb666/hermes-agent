# UModelVerse Integration for hermes-agent

[UModelVerse](https://astraflow.ucloud.cn/) is a model service platform by **UCloud** (优刻得),
providing a single OpenAI-compatible API endpoint to access 200+ mainstream AI models —
including DeepSeek, GPT-4o, Claude, Gemini, Qwen, and more.

No infrastructure management needed. Pay only for what you use.

## Quick Start

**Step 1**: Get your free API key at [astraflow.ucloud.cn](https://astraflow.ucloud.cn/)

**Step 2**: Install the OpenAI SDK (if not already installed)

```bash
pip install openai
```

**Step 3**: Point this project to UModelVerse — change only the `base_url`:

```python
from openai import OpenAI

client = OpenAI(
    api_key="your-umodelverse-api-key",
    base_url="https://api.modelverse.cn/v1",
)

response = client.chat.completions.create(
    model="deepseek-ai/DeepSeek-V3.2",
    messages=[{"role": "user", "content": "Hello!"}],
)
print(response.choices[0].message.content)
```

## Available Models (sample)

| Model | Type |
|-------|------|
| `deepseek-ai/DeepSeek-V3.2` | Text generation, cost-effective |
| `deepseek-ai/DeepSeek-R1` | Deep reasoning |
| `gpt-4o` | OpenAI GPT-4o |
| `claude-opus-4-6` | Anthropic Claude |
| `gemini-2.5-pro` | Google Gemini |
| `Qwen/Qwen3-235B-A22B` | Alibaba Qwen |

Full model list: [docs.ucloud.cn/umodelverse](https://docs.ucloud.cn/umodelverse/api/chat_completions)

## Why UModelVerse?

- **200+ models** via one API key — switch models without changing your code
- **OpenAI-compatible** — works with any existing OpenAI SDK integration
- **Pay-per-use** — no long-term commitment, no idle compute cost
- **Fine-grained billing** — track usage per API key and per model
- **Enterprise-grade** — IP whitelist, budget controls, sub-account management

> Auto-generated by UModelVerse GitHub Agent
